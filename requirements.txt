# ── Fapello (Cloudflare JS-challenge bypass without a full browser) ──────────
cloudscraper>=1.2.71    # handles CF IUAM/JS-challenge from datacenter IPs
requests>=2.32.0        # cloudscraper depends on this
brotlicffi>=1.0.0       # decode brotli-encoded CF responses correctly

# ── HTML parsing ─────────────────────────────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.1.0

# ── Bunkr (full undetected browser required for CF Bot Management) ────────────
nodriver>=0.36          # async undetected Chrome, successor to undetected-chromedriver
